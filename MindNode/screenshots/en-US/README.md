`snapshot` will generate and store new screenshots in this folder. Running `frameit white` will generate the nice frames with the title on top.

`.strings` files must be UTF-16 encoded, or generated with Xcode.
